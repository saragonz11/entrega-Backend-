# Copia este archivo como .env y pega tu URI de MongoDB Atlas.
# Incluye el nombre de la base de datos en la ruta (antes del ?), por ejemplo:
# mongodb+srv://USUARIO:CONTRASEÑA@cluster.mongodb.net/NOMBRE_DB?retryWrites=true&w=majority&appName=Cluster0

MONGODB_URI=
